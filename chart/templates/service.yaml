apiVersion: v1
kind: Service
metadata:
  name: {{ include "reflow-gateway.fullname" . }}
  labels:
    {{- include "reflow-gateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "reflow-gateway.selectorLabels" . | nindent 4 }}
